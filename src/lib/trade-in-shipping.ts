/** Where sellers ship trade-ins — shown in the wizard, the /trade-in page and
 * the offer-locked email. TODO: owner to provide the real intake address. */
export const TRADE_IN_SHIP_TO = {
  name: "reMint Trade-ins",
  lines: ["[Street address — to be provided]", "[Unit / suite]", "[City, State ZIP]"],
};
